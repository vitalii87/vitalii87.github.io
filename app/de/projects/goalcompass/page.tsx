import { LocalizedProjectPage, projectMetadata } from '../../../components/LocalizedPages';
export const metadata = projectMetadata('de', 'goalcompass');
export default function Page() { return <LocalizedProjectPage lang="de" slug="goalcompass" />; }
