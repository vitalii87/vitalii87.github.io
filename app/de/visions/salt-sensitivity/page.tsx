import { LocalizedVisionPage, visionMetadata } from '../../../components/LocalizedPages';
export const metadata = visionMetadata('de', 'salt-sensitivity');
export default function Page() { return <LocalizedVisionPage lang="de" slug="salt-sensitivity" />; }
