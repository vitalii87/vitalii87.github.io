import { LocalizedVisionPage, visionMetadata } from '../../../components/LocalizedPages';

export const metadata = visionMetadata('de', 'origin-attenuation');

export default function Page() {
  return <LocalizedVisionPage lang="de" slug="origin-attenuation" />;
}
