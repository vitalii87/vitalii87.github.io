import { LocalizedVisionPage, visionMetadata } from '../../../components/LocalizedPages';
export const metadata = visionMetadata('de', 'relational-narrowing');
export default function Page() { return <LocalizedVisionPage lang="de" slug="relational-narrowing" />; }
