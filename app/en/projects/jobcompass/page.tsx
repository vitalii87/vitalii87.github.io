import { LocalizedProjectPage, projectMetadata } from '../../../components/LocalizedPages';
export const metadata = projectMetadata('en', 'jobcompass');
export default function Page() { return <LocalizedProjectPage lang="en" slug="jobcompass" />; }
