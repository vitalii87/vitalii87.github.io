import { LocalizedVisionPage, visionMetadata } from '../../../components/LocalizedPages';

export const metadata = visionMetadata('en', 'origin-attenuation');

export default function Page() {
  return <LocalizedVisionPage lang="en" slug="origin-attenuation" />;
}
