import { LocalizedVisionPage, visionMetadata } from '../../../components/LocalizedPages';
export const metadata = visionMetadata('en', 'salt-sensitivity');
export default function Page() { return <LocalizedVisionPage lang="en" slug="salt-sensitivity" />; }
